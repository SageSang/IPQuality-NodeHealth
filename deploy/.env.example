# Copy this file to the repository root as .env, then adjust it for this
# Synology installation. Required empty values intentionally block startup.
PUID=1026
PGID=100
TZ=Asia/Shanghai

NODE_HEALTH_STORAGE_ROOT=/volume1/docker/YOUR_PROJECT_DIR
NODE_HEALTH_CONFIG_PATH=/volume1/docker/YOUR_PROJECT_DIR/config/config.yaml
MIHOMO_BOOTSTRAP_PATH=/volume1/docker/YOUR_PROJECT_DIR/config/mihomo-bootstrap.yaml
NODE_HEALTH_BIND_IP=192.0.2.2
NODE_HEALTH_PORT=18887
# OpenWrt LAN address written into reports for local SOCKS5 connections.
LOCAL_SOCKS_ADVERTISE_HOST=192.0.2.4
NODE_HEALTH_NAMESPACE=node-health-production
# Choose and review a stable unique instance ID before enabling fixed-port maps.
NODE_HEALTH_SERVER_INSTANCE_ID=
# Generate a long random value before starting the project. This protects
# run/audit APIs and detailed report downloads; Compose rejects an empty value.
NODE_HEALTH_API_TOKEN=
# Optional but recommended for complete Claude split-route risk evidence.
IPINFO_TOKEN=
IPAPI_KEY=

PYTHON_IMAGE=python:3.12.11-slim-bookworm
MIHOMO_IMAGE=metacubex/mihomo:v1.19.29
# Pin a reviewed image digest for migration and compatible rollback.
NODE_HEALTH_IMAGE=ghcr.io/sagesang/ipquality-node-health@sha256:REPLACE_WITH_REVIEWED_DIGEST
SUB_STORE_INVENTORY_URL=http://192.0.2.2:3001/download/collection/inventory?target=ClashMeta&noCache=true
